apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Values.appName }}-service
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: {{ .Release.Name }}-{{ .Values.appName }}