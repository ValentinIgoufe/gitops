apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: linkerd-identity-trust-roots
  namespace: cert-manager
spec:
  sources:
  - secret:
      name: linkerd-trust-anchor-key-pair
      key: "ca.crt"
  target:
    configMap:
      key: "ca-bundle.crt"